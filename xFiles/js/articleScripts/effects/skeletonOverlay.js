
	// (function(){

	// 	var targets = Array.from(document.getElementsByClassName('x'));

	// 	targets.map(function(item){
	// 		var item_width = item.offsetWidth;
	// 		console.log({item , item_width});
	// 		var x = vw(item_width)/5 + 'vw';
	// 		item.style.setProperty('--x', x);
	// 		//item.style.fontSize = (item_width/5) + 'px';
	// 	});

	// 	function vw(v) {
	// 	  var w = Math.max(document.documentElement.clientWidth, window.innerWidth || 0);

	// 	  return (v / w) * 100;
	// 	}

	// })();